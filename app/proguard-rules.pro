# Android ProGuard rules for release builds
# Add project-specific ProGuard rules here.
# By default, the file in the Android SDK is used (tools/proguard/proguard-android-optimize.txt).

# Example rules:
# -keep class com.example.simpleapp.** { *; }
